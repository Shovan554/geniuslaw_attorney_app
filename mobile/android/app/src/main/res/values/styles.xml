<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#FFFFFF</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
  <!-- Stripe Identity's IdentityActivity uses Compose + the Material theme adapter,
       which requires a host theme extending Theme.MaterialComponents. Scoped to that
       activity only so the rest of the app keeps its AppCompat theme. -->
  <style name="Theme.StripeIdentity" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
</resources>